# 阶段 6 实施记录（学习复习 + AI 推送）

> 状态：✅ 已落地（2026-06-25 完结）
> 配套设计：docs/10-架构/面试题库设计.md + AI推送设计.md
> 配套技术：docs/10-架构/面试题库-技术设计.md + AI推送-技术设计.md
> 配套页面：docs/10-架构/面试题库-页面规划.md + AI推送-页面规划.md
> DOD 验证（CLAUDE.md 1.8 节）：✅ 全部通过

---

## 一、整体范围

按 Stage 5 决策：
- ✅ D2 resume_summary → LLM 真摘要
- ✅ D5 submit_answer → 自动同步 progress
- ⏭️ D1 语音架构（用户说"已经搞定"，跳过）
- ❌ D3 Interview.related_question_ids（用户决策"不做"）
- ✅ D4 50 道种子题复用（per CLAUDE.md 二）

实施分 5 个 Phase：

```
Phase 0  前置收尾        (8 commit)  ✅
Phase 1  后端骨架        (12 表 + 4 services + 30 endpoints) ✅
Phase 2  前端页面        (/learn /review /qa + dashboard)   ✅
Phase 2-5 题目详情      (/learn/[qid] 详情页)              ✅
Phase 3  联调 + 端到端    (E2E happy path 10 步)            ✅
```

---

## 二、Phase 0 前置收尾（8 commit）

| 主题 | commit | 内容 |
|---|---|---|
| 文档 4 层重构 | ef27265 | docs 重命名 + 4 层目录 + 面试题库 / AI 推送设计落地 |
| bug fix | eafce6a | StrictMode dedup + history 搜索 + favorite |
| feat(profile) | 6c9e6af | PDF 简历上传 + LLM 抽取 + 侧栏预览 |
| feat(voice) | d48d06d | WebSocket PTT + LiveKit worker tracking |
| chore | 4225f3e | package reorder + .gitignore |
| feat(ui) | f7b6d2c | digest + learn 组件 + 类型 (Phase 2 prep) |
| chore | 54dea3b | main 注册 voice_ws_router |
| refactor | cfa7b58 | api.ts 死代码清理 |
| fix | 6c37227 | import QuestionListFilter |

**自检修复**：QuestionTag generated column 冲突、archive task reference、cache private API → commit 0b43487

---

## 三、Phase 1 后端骨架（3 commit）

### 1a · 12 新表 + Redis + 分区 + 归档（commit 3ebfe8e + 0b43487）

**12 表**（5 learn + 6 候选 + 1 归档）：
- learn: question_progress / learning_sessions / review_schedules / study_plans / monthly_reports
- 候选: question_tags / question_tag_maps / user_question_notes / bookmark_collections / question_answer_logs / user_questions
- 归档: question_progress_archive

**Profile 扩字段**：weak_topics / mastered_topics / learning_trajectory / last_active_at / digest_stats

**Redis 集成**：
- docker-compose.yml: redis:7-alpine
- requirements.txt: redis>=5.0
- core/cache.py: 薄封装 get/set/delete/delete_pattern/incr + 优雅降级

**HASH 分区**：question_progress 按 user_id 散 16 partition（Phase 3 时 MySQL 9 禁用，留 TODO）

**冷数据归档**：services/archive_service.py 月度 cron，mastered > 1Y 迁 archive

### 1b · 4 services + SM-2 + QASession（commit 3a1b2fa）

- `question_bank_service.py` (440 行)：题库查询 / 用户题 CRUD / tag / note
- `learning_progress_service.py` (516 行)：**SM-2 算法** + review_queue (Redis cache) + upsert_from_interview (D5)
- `study_plan_service.py` (173 行)：计划 CRUD + 真实进度聚合
- `qa_service.py` (228 行)：LLM 1v1 问答 + 会话管理

### 1c · 30 learn endpoints + 43 schemas（commit 3277c3d + 2919dc8）

`backend/api/learn.py` (502 行) + `backend/schemas/learn.py` (355 行)：

```
题目 (5)          Session (3)         学习计划 (5)
GET  /questions    POST /sessions      GET  /plans
GET  /questions/{qid}   PATCH /sessions/{sid}/end    POST /plans
POST /questions/{qid}/answer   GET /sessions/recent    PATCH /plans/{pid}
PATCH /progress/{qid}                            DELETE /plans/{pid}
GET  /progress                                 GET /plans/{pid}/progress

QA (5)            Tag/Note/UserQ (6)  推荐/统计 (2)
GET  /qa          GET  /tags          GET /recommend
POST /qa          POST /tags          GET /review-queue
GET  /qa/{sid}    POST /questions/{qid}/tags/{tid}
DELETE /qa/{sid}  DELETE /questions/{qid}/tags/{tid}
POST /qa/chat     GET /questions/{qid}/note / PUT
                  POST /user-questions / PUT / DELETE
```

### 1d · D2 LLM 真摘要（commit e2771c7）

```python
# resume_parser.py
extract_profile_from_text() 加 summary 字段:
  "summary": "候选人画像 (200字以内, 第三人称, 中文)"

# 兜底: LLM 没返回 → 从 extracted fields 自动拼
```

### 1e · D5 自动同步（commit e2771c7）

```python
# api/interview.py:submit_answer 末尾
if record.question_id:
    await upsert_from_interview(
        db, user_id, question_id=record.question_id,
        correct=(record.score or 0) >= 3,
        interview_id=interview_id,
    )
```

---

## 四、Phase 2 前端页面（3 commit）

### 2 · 3 个新 tab + dashboard（commit 58ec5cf）

- `/learn` (250 行)：题库列表 + 6 维过滤
- `/review` (249 行)：SM-2 复习队列 + 6 档自评
- `/qa` (244 行)：LLM 1v1 问答界面
- `dashboard.tsx` (+8 行)：学习复习卡片 + 导航入口

### 2-5 · 题目详情页（commit 79a3dba）

`/learn/[qid].tsx` (502 行)：
- 题目展示 + 进度卡片 + 答题 + 收藏切换 + 笔记编辑
- 追问树调试折叠面板

---

## 五、Phase 3 联调（commit pending）

**E2E happy path 10 步全部通过**：

| # | 步骤 | 验证 |
|---|---|---|
| 1 | POST /api/auth/register | token 颁发 |
| 2 | Auth gate | 401 ✓ |
| 3 | GET /api/learn/questions | total=50, items=20 |
| 4 | GET /api/learn/questions/agent_001 | 5 key_points |
| 5 | POST /api/learn/questions/agent_001/answer | SM-2 算 status=learning, ease=2.5 |
| 6 | GET /api/learn/progress | 1 progress saved |
| 7 | PATCH /api/learn/progress/agent_001 | bookmarked=true |
| 8 | GET /api/learn/review-queue | (空, 未到期) |
| 9 | GET /api/learn/stats | 1 practice, 100% accuracy |
| 10 | GET /api/learn/recommend | (无新题) |

**修复 6 个 E2E bug**：
1. repetition_count → review_count 字段对齐
2. QuestionProgress 新行默认值（server_default 缺失）
3. list_my_progress dict 缺 id 字段
4. main.py _archive_task 跨模块赋值失败
5. QuestionTag.user_id FK 移除（MySQL 9 不兼容）
6. HASH 分区 / INCLUDE 语法禁用（MySQL 9 不支持）

---

## 六、测试覆盖

| 端 | 框架 | 测试数 | 状态 |
|---|---|---|---|
| 后端 | pytest | **92** | ✅ |
| 前端 | vitest + RTL | **67** | ✅ |
| E2E | bash + curl | **10** | ✅ |
| **总计** | | **169** | ✅ |

CLAUDE.md 第六节「单测强制」规则已建立（所有代码 commit 必带单测）。

---

## 七、留给后续阶段

### 7.1 已记录到 目前缺陷.md 的债务

- **债务 8**：`question_progress.user_id` 无 FK，应用层保证引用完整性（建议建 `UserService.delete_user`）
- **Bug 9**：SM-2 测试与函数签名不一致（已修，记录保留作历史）

### 7.2 后续阶段可能要做

- **AI 推送 8 张表 + 7 个页面**：用户说"后期优化时再设计"
- **`/learn/[qid]` 详情页 UI 增强**：当前已能跑，加分项是练习历史曲线
- **Phase 3 Redis 容器化**：`docker-compose up redis` 应能跑（当前用 brew 临时）
- **议题 C 语音架构收口**：当前 LiveKit + WebSocket PTT 并存，留 P1 阶段处理

---

## 八、commit 清单

```
阶段 6 共 ~20 个 commit，按时间顺序：
0b43487 fix(phase1a): 自查 3 个 bug + 1 个 tech debt 文档化
3ebfe8e feat(backend): Phase 1a · 12 新表 + Redis 集成 + HASH 分区 + 冷归档
3a1b2fa feat(services): Phase 1b · 4 services + SM-2 算法 + QASession 表
3277c3d feat(api): Phase 1c · learn router + schemas (22 endpoints)
2919dc8 fix(learn): 自查 Phase 1c · 4 类修复
e2771c7 feat(phase1de): D2 + D5 落地
99be5d9 fix(learn): get_recommend 占位 SQL 删除
9cecf0e test(全栈): Phase 1+2 单测 + CLAUDE.md 加单测强制规则
58ec5cf feat(ui): Phase 2 · 学习复习 3 tab + dashboard 集成
79a3dba feat(ui): Phase 2-5 · /learn/[qid] 题目详情页 + 单测
Phase 3 commit  ← (see git log)
```